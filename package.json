{
  "name": "trade-ai-demo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/server"
  ],
  "scripts": {
    "dev": "concurrently -n web,server -c blue,green \"npm run dev --workspace @trade-ai-demo/web\" \"npm run dev --workspace @trade-ai-demo/server\"",
    "dev:web": "npm run dev --workspace @trade-ai-demo/web",
    "dev:server": "npm run dev --workspace @trade-ai-demo/server",
    "build": "npm run build --workspace @trade-ai-demo/web && npm run build --workspace @trade-ai-demo/server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
